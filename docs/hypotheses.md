# 假设隔离台账

未验证的外部系统理论只住这里（check_provenance.py 豁免本文件），一行一假设，防止未验证理论经写入放大污染代码/ADR/文档。
卡壳 2 轮或会话重载后先读本台账：与表冲突的上下文断言降级为待验证。

断言分级：`活跃`（待验证）→ `单次观测`（可能是特定环境巧合，推广前须复现或文档佐证）→ `已验证`（原始输出/源码/文档原文/用户确认背书，注来源）→ `已证伪`（注证据；证伪即 grep 全仓清零相关标识符）。

| # | 假设 | 当时依据 | 证伪条件 | 状态 | 证据去向 |
|---|------|---------|---------|------|---------|
| （暂无） | | | | | |
